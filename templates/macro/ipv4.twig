{%- macro asn(name, n) -%}
    {% if n %}{{n}} / {{ name }}{% else %}{{ name }}{% endif %}
{%- endmacro -%}

{%- macro ip_search(ip) -%}
    <a href="https://whatismyipaddress.com/ip/{{ ip }}" title="whatismyipaddress.com" class="brackets tooltip"><span class="wimia">WI</span></a>
    <a href="tools.php?action=ip_search&amp;ip={{ ip }}">{{ ip }}</a>
{%- endmacro -%}
